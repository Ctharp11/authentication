import React from 'react';
import { withRouter } from 'react-router-dom';
// import { logout } from '../utils/requests';

const Navbar = (props) => {
    // const { user } = props;
    // const username = user ? user.username : 'Cameron';
    // const handleLogIn = () => {
    //     props.history.push('/login');
    // };
    // const handleLogOut = () => {
    // logout()
    // }
    return (
        <div> Navbar</div>
    )
}

export default withRouter(Navbar);
